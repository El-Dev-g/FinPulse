// src/app/dashboard/link-account/connect/layout.tsx
import React from 'react';

// This layout is designed to be minimal, to not interfere with the connection flow UI.
export default function ConnectLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
